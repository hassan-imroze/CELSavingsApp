@{
    ViewBag.Title = "Account Holder Dashboard";
}

<h2>Account Holder Dashboard</h2>

@*<div class="row">
        <hr style="width: 100%; color: #EEEEEE; height: 0.3px; background-color:#EEEEEE;" />
    </div>*@


<div class="row">
    <div class="col">

        <h4 style="margin-top:20px;">Account Balance:</h4>
        <hr />
        <div class="row">
            <table id="tblSavingsAccounts" class="table table-bordered table-hover">
                <thead class="thead-light">
                </thead>
                <tbody></tbody>
            </table>
        </div>

    </div>
    <div class="col">
        <h4 style="margin-top:20px;">Recent Payments(Last 5):</h4>
        <hr />
        <div class="row">

            <table id="tblRecentPayments" class="table table-bordered table-hover">
                <thead class="thead-light">
                </thead>
                <tbody></tbody>
            </table>
        </div>
    </div>
</div>



@section scripts
{
    @Scripts.Render("~/bundles/datatables");
    <script>
        $(document).ready(function () {
            var table = $("#tblSavingsAccounts").DataTable({
                paging: false,
                info: false,
                searching: false,
                ajax: {
                    url: "/api/savingaccounts/byEmailAddress/@ViewData["UserEmail"]/",
                    method: "GET",
                    dataSrc: ""
                },

                columns: [
                    {
                        data: "accountNo",
                        title: "AccountNo."
                    },
                    {
                        data: "name",
                        title: "Name"
                    },
                    {
                        data: "balance",
                        className: "text-right",
                        render: $.fn.dataTable.render.number(',', '.', 4, ''),
                        title: "Balance"
                    },
                    {
                        data: "id",
                        className:"text-center",
                        title: "Statement",
                        render: function (data, type, obj) {
                            return "<a class='btn btn-outline-primary btn-sm' href='/transactions/accountStatement/" + data + "' target='_blank'>View</a>";
                        }
                    }
                ]
                //{
                //    data: "id",
                //    title: "Delete",
                //    render: function (data, type, movie) {
                //        return "<button data-movie-id='" + data + "' data-movie-name='" + movie.name + "' class='btn btn-sm btn-danger js-delete'>Delete</button>";
                //    }
                //}

            });

            var table = $("#tblRecentPayments").DataTable({
                ajax: {
                    url: "/api/payments/latestByEmailAddress/@ViewData["UserEmail"]/",
                    method: "GET",
                    dataSrc: ""
                },
                paging: false,
                info: false,
                searching: false,
                columns: [
                    {
                        data: "accountNo",
                        title: "AccountNo."
                    },
                    {
                        data: "paymentMonth",
                        title: "Month"
                    },
                    {
                        data: "paymentAmount",
                        className: "text-right",
                        render: $.fn.dataTable.render.number(',', '.', 4, ''),
                        title: "Amount"
                    }
                ]
                //{
                //    data: "id",
                //    title: "Delete",
                //    render: function (data, type, movie) {
                //        return "<button data-movie-id='" + data + "' data-movie-name='" + movie.name + "' class='btn btn-sm btn-danger js-delete'>Delete</button>";
                //    }
                //}

            });
            //$("#tblSavingsAccounts").on("click", ".js-delete", function () {
            //    var button = $(this);
            //    console.log(button.attr("data-movie-id"));

            //    bootbox.confirm({
            //        title: "Delete Movie ?",
            //        message: "Sure you want to delete this movie with name: <b>'" + button.attr("data-movie-name") + "'</b> ?",
            //        closeButton: false,
            //        backdrop: true,
            //        callback: function (result) {
            //            console.log(result);
            //            if (result) {
            //                $.ajax({
            //                    url: "/api/movies/" + button.attr("data-movie-id"),
            //                    method: "DELETE",
            //                    success: function () {
            //                        table.row(button.parents("tr")).remove().draw();
            //                    }
            //                });
            //            }
            //        }
            //    });
            //});
        });
    </script>
}

